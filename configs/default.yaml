camera:
  source: 0
  width: 1280
  height: 720
  fps: 30
  backend: dshow

detector:
  model: yolo26n.pt
  device: auto
  precision: auto
  imgsz: 640
  confidence: 0.25
  max_det: 100
  warmup_iterations: 10

telemetry:
  rolling_window_frames: 120

display:
  window_name: "Vision Tracking System"
  show_crosshair: true